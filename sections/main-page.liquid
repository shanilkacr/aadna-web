<section class="page page-width">
  <h1>{{ page.title }}</h1>
  <div class="page__content">{{ page.content }}</div>
</section>

{% schema %}
{
  "name": "Page",
  "enabled_on": {
    "templates": ["page"]
  },
  "settings": []
}
{% endschema %}
